FROM debian:bookworm-slim

WORKDIR /app

# Placeholder image. Update with the chosen runtime and dependencies.
CMD ["bash", "-lc", "echo 'Container image placeholder. Implement app runtime.' && sleep infinity"]
